[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd electricity-deployment/backend && python -m uvicorn api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
